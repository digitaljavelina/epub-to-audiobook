[project]
name = "epub-to-audiobook"
version = "0.2.0"
description = "Convert EPUB files into chaptered M4B audiobooks using gpt-audio via OpenRouter"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "ebooklib>=0.20",
    "requests>=2.32",
]
